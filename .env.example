# DRIBL API Authentication (if required)
# Capture bearer token from an authenticated DRIBL browser session
# (Chrome DevTools → Network → look for Authorization header)
DRIBL_TOKEN=your_bearer_token_here
